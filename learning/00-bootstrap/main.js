const log = document.getElementById('log');
log.textContent = 'Step 00 ready. Open the console.';

const map = document.getElementById('map');
map.textContent = 'Map container (empty for now).';

console.log('Step 00: bootstrap loaded');
